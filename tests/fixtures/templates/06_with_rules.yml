stages:
  - build
  - deploy

build:
  stage: build
  script:
    - make build
  rules:
    - if: $CI_COMMIT_BRANCH

deploy:
  stage: deploy
  script:
    - make deploy
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: manual
